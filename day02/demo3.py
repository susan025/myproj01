if __name__ == '__main__':
    #求列表的长度
    mlist = [1,False,3,"ha",True,5]
    mlen = len(mlist)
    print("列表的长度是：{mlen}".format(mlen = mlen))

    # 求列表的长度
    nlist = [1, 2, 3, "ha", True]
    nlen = len(nlist)
    print("列表的长度是：{nlen}".format(nlen=nlen))

    # 求列表的长度
    slist = [1, 2, 3, "ha", True,"嘿嘿"]
    slen = len(slist)
    print("列表的长度是：{slen}".format(slen=slen))

    # 求列表的长度
    rlist = [1, 2, 3, "ha", True,False,"呵呵",666]
    rlen = len(rlist)
    print("列表的长度是：{rlen}".format(rlen=rlen))